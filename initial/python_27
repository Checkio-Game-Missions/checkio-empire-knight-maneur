def knight(cells):
    return 0


if __name__ == "__main__":
    # These "asserts" using only for self-checking and not necessary for auto-testing
    assert knight("b1-d5") == 2, "1st example"
    assert knight("a6-b8") == 1, "2nd example"
    assert knight("h1-g2") == 4, "3rd example"
    assert knight("h8-d7") == 3, "4th example"
    assert knight("a1-h8") == 6, "5th example"